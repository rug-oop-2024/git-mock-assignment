import numpy as np

if __name__ == '__main__':
    numbers = [n for n in range(1, 11)]
    numbers_array = np.array(numbers)
    
    
    numbers_array = np.array(numbers)
    print(numbers_array)